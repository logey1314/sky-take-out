<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderMapper">


    <insert id="insert" parameterType="Orders" useGeneratedKeys="true" keyProperty="id">
        insert into orders (number, status, user_id, address_book_id, order_time, checkout_time, pay_method, pay_status, amount, remark, phone, address, consignee, estimated_delivery_time, delivery_status, pack_amount, tableware_number, tableware_status)
        values (#{number}, #{status}, #{userId}, #{addressBookId}, #{orderTime}, #{checkoutTime}, #{payMethod}, #{payStatus}, #{amount}, #{remark}, #{phone}, #{address}, #{consignee}, #{estimatedDeliveryTime}, #{deliveryStatus}, #{packAmount}, #{tablewareNumber}, #{tablewareStatus})
    </insert>

    <update id="update" parameterType="com.sky.entity.Orders">
        update orders
        <set>
            <if test="cancelReason != null and cancelReason!='' "> cancel_reason=#{cancelReason}, </if>
            <if test="rejectionReason != null and rejectionReason!='' "> rejection_reason=#{rejectionReason}, </if>
            <if test="cancelTime != null"> cancel_time=#{cancelTime}, </if>
            <if test="payStatus != null"> pay_status=#{payStatus}, </if>
            <if test="payMethod != null"> pay_method=#{payMethod}, </if>
            <if test="checkoutTime != null"> checkout_time=#{checkoutTime}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="deliveryTime != null"> delivery_time = #{deliveryTime} </if>
        </set>
        where id = #{id}
    </update>
    <select id="getHistoryOrder" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            <if test="userId !=null">
                and user_id=#{userId}
            </if>
            <if test="status !=null and status != ''">
                and status=#{status}
            </if>
        </where>
    </select>


    <resultMap id="OrderVOMap" type="com.sky.vo.OrderVO">
        <id property="id" column="id"/>
        <result property="number" column="number"/>
        <result property="status" column="status"/>
        <result property="userId" column="user_id"/>
        <result property="addressBookId" column="address_book_id"/>
        <result property="orderTime" column="order_time"/>
        <result property="checkoutTime" column="checkout_time"/>
        <result property="payMethod" column="pay_method"/>
        <result property="payStatus" column="pay_status"/>
        <result property="amount" column="amount"/>
        <result property="remark" column="remark"/>
        <result property="userName" column="user_name"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="consignee" column="consignee"/>

        <!-- 一对多关联查询订单明细 -->
        <collection property="orderDetailList" ofType="com.sky.entity.OrderDetail">
            <id property="id" column="detail_id"/>
            <result property="name" column="detail_name"/>
            <result property="image" column="detail_image"/>
            <result property="orderId" column="order_id"/>
            <result property="dishId" column="dish_id"/>
            <result property="setmealId" column="setmeal_id"/>
            <result property="dishFlavor" column="dish_flavor"/>
            <result property="number" column="detail_number"/>
            <result property="amount" column="detail_amount"/>
        </collection>
    </resultMap>

    <select id="getHistoryOrderByResultMap" resultMap="OrderVOMap">
        SELECT
        o.id, o.number, o.status, o.user_id, o.address_book_id,
        o.order_time, o.checkout_time, o.pay_method, o.pay_status,
        o.amount, o.remark, o.user_name, o.phone, o.address, o.consignee,
        od.id as detail_id, od.name as detail_name, od.image as detail_image,
        od.order_id, od.dish_id, od.setmeal_id, od.dish_flavor,
        od.number as detail_number, od.amount as detail_amount
        FROM orders o
        LEFT JOIN order_detail od ON o.id = od.order_id
        <where>
            <if test="userId != null">
                AND o.user_id = #{userId}
            </if>
            <if test="status != null and status != ''">
                AND o.status = #{status}
            </if>
        </where>
        ORDER BY o.order_time DESC
    </select>

    <select id="orderSearch" resultType="com.sky.entity.Orders">
        select * from orders
        <where>
            <if test="beginTime !=null and beginTime!=''">
                  and order_time >=#{beginTime}
            </if>
            <if test="endTime !=null and endTime!=''">
                  and order_time &lt;= #{endTime}
            </if>

            <if test="number !=null and number!=''">
                and  number=#{number}
            </if>
            <if test="phone !=null and phone!=''">
                and phone=#{phone}
            </if>
            <if test="status !=null and status!=''">
                and status =#{status}
            </if>
        </where>
        order by order_time desc
    </select>

    <select id="getTurnOver" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="beginTime != null">
                and order_time &gt; #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt; #{endTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>


    <select id="getOrderCount" resultType="java.lang.Integer">
        select COUNT(id) from orders
        <where>
            <if test="beginTime != null">
                and order_time &gt; #{beginTime}
            </if>
            <if test="endTime != null">
                and order_time &lt; #{endTime}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>


    <select id="getSalesTop" resultType="com.sky.dto.GoodsSalesDTO">
        SELECT od.name, SUM(od.number) AS number
        FROM order_detail od, orders o
        WHERE od.order_id = o.id
        AND o.status = 5
        <if test="beginTime != null">
            AND o.order_time &gt; #{beginTime}
        </if>
        <if test="endTime != null">
            AND o.order_time &lt; #{endTime}
        </if>
        GROUP BY od.name
        ORDER BY number DESC
        LIMIT 0, 10
    </select>

    <select id="sumByMap" resultType="java.lang.Double">
        select sum(amount) from orders
        <where>
            <if test="begin != null"> and order_time > #{begin} </if>
            <if test="end != null"> and order_time &lt; #{end} </if>
            <if test="status != null"> and status = #{status} </if>
        </where>
    </select>
    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="begin != null"> and order_time > #{begin} </if>
            <if test="end != null"> and order_time &lt; #{end} </if>
            <if test="status != null"> and status = #{status} </if>
        </where>
    </select>


</mapper>